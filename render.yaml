services:
  - type: web
    name: financial-backend
    runtime: node
    buildCommand: apt-get update && apt-get install -y --no-install-recommends tesseract-ocr poppler-utils && npm install
    startCommand: npm start